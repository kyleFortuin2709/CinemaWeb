{
  "name": "CinemaWeb",
  "version": "0.1.0",
  "scripts": {
    "start:server": "cross-env NODE_ENV=dev nodemon server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "cross-env": "^7.0.3"
  }
}
